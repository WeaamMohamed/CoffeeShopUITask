import 'package:flutter/cupertino.dart';

class CoffeeProfileScreen extends StatelessWidget {
  const CoffeeProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Profile screen'),
      ),
    );
  }
}
